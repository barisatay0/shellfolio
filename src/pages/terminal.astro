---
import Layout from "../layouts/Layout.astro";
import TerminalComponent from "../components/Terminal/index.astro";
import { getLangFromUrl, useTranslations } from "../translation/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title={t('terminal.title')}>
    <TerminalComponent />
</Layout>
